# 变更记录

## 1.0.6

- 默认安装使用 GHCR 镜像 `pms-front` 与 `pms-backend` 的 `1.0.6`。
- 首次空库固定初始管理员：`admin@example.com` / `PmsAdmin123!`（写入 `.env.example` 与 README；`bootstrap.sh` 不再随机生成管理员密码，仅随机 MySQL/JWT）。生产配置须更换该密码。
- 邀请员工时初始角色来自角色管理中的启用角色；人员归属弹窗与项目节点负责人默认值等体验修复。

## 1.0.5

- 默认安装使用 GHCR 镜像 `pms-front` 与 `pms-backend` 的 `1.0.5`。
- 默认安装路径改为 MySQL 8.4。Flyway 随后端启动，启动脚本内存下限为 2 GiB。
- 后端镜像包含 utf8mb4 兼容 Flyway 脚本，空 MySQL 库可以完成初始化。
- 项目列表支持「我负责的 / 公司组合 / 全部」视图与关注筛选。
- 选人框可搜索全公司已激活账号；指派后自动加入项目成员，且不会覆盖并发加入。
- 支持重发待处理邀请。
- 增加 GitHub 校验与发布工作流、安装/功能 Issue 模板、隐私扫描，以及匿名 GHCR 镜像验证。
- 提供面向产品的 README、演示截图和完整 Apache-2.0 许可证文本。
- 示例 CORS 对齐 `127.0.0.1`；`bootstrap.sh` 发现非默认 `PMS_PORT` 时会改写占位 CORS 和公网地址。

## 1.0.2

- 首次公开发布：固定版本 Compose，以及启动、状态、日志、备份、恢复和升级脚本。
- 默认安装使用 GHCR 镜像 `pms-front` 与 `pms-backend` 的 `1.0.2`。
- 启动脚本把缺失密钥写入本地、未跟踪的 `.pms-bootstrap-secrets`；Docker 内存不足时拒绝启动。
- 后端和数据库端口只留在容器网络，仅发布前端入口端口。
- 该版本默认路径为当时的发行栈；当前未发布改动已改为只支持 MySQL 8.4。
